[project]
name = "digital-lluc-bot"
version = "0.1.0"
description = "digital-lluc — the voice/text agent behind Lluc's website"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[openai,cartesia,deepgram,silero,daily,webrtc,runner]~=1.5.0",
    "pyyaml~=6.0",
]
